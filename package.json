{
  "name": "toolbox",
  "private": true,
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev",
    "predev:open-uni": "lsof -ti:3001,3002 | xargs kill -9 2>/dev/null; true",
    "dev:open-uni": "turbo run dev --filter=@toolbox/open-uni-recorder-api --filter=@toolbox/open-uni-recorder-web --parallel",
    "lint": "turbo run lint",
    "format": "prettier --write \"**/*.{ts,tsx,md}\"",
    "check-types": "turbo run check-types"
  },
  "devDependencies": {
    "prettier": "^3.7.4",
    "turbo": "^2.9.14",
    "typescript": "5.9.2"
  },
  "packageManager": "pnpm@9.0.0",
  "engines": {
    "node": ">=18"
  }
}
